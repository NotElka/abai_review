// Продакшн-сборка (используется при `ng build`).
// ВАЖНО: после создания бэкенда на Render замените apiUrl на ваш адрес,
// например 'https://abai-review-backend.onrender.com' (без слэша в конце).
export const environment = {
  production: true,
  apiUrl: 'https://abai-review-backend.onrender.com',
};
